# YouTrack Left Issue Sidebar Extension

Simple browser extension for YouTrack that moves the **issue fields sidebar**  
from the right side to the left on the **issue page / issue view**.

# Installation

This extension is configured to work with **youtrack.jetbrains.com**.  
If you want to use it with your own YouTrack instance, update the domain in `manifest.json`.

## GitHub

- **Code** → **Download ZIP**
- Unzip the archive
- *(Optional)* Open `manifest.json` and replace `youtrack.jetbrains.com` with the domain of your own YouTrack instance

## Chrome

- Open **Extensions**
- Go to **Manage Extensions**
- Enable **Developer mode** (top-right corner)
- Click **Load unpacked**
- Select the extension directory
